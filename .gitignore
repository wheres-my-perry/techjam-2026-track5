# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# Node
node_modules/
dist/
.next/

# ML artifacts
*.ckpt
*.pt
*.pth
*.onnx
*.h5
wandb/
runs/
mlruns/

# Data (keep repo light; link large data instead)
data/*
!data/.gitkeep
!data/README.md

# Env & IDE
.env
.env.*
.DS_Store
.vscode/
.idea/

# embedding caches (regenerable, large)
outputs/**/cache/

# Local standing orders / private notes — never pushed (public repo)
AGENTS.md
HANDOFF.md
HANDOFF_COWORK.md

# Submission working files — kept local
deliverables/

# Training artifacts: 48 GB of checkpoints and eval dumps. *.pt was already ignored but the
# surrounding tree was not, so a blind "git add -A" would have tried to commit gigabytes.
# The measured NUMBERS live in logs/ and docs/, which are committed. (2026-09-01)
outputs/

# Training artifacts: 48 GB of checkpoints and eval dumps. *.pt was already ignored but the
# surrounding tree was not, so a blind "git add -A" would have tried to commit gigabytes.
# The measured NUMBERS live in logs/ and docs/, which are committed. (2026-09-01)
outputs/

# Superseded material and private notes — kept locally, never pushed (public repo)
archive/
CLAUDE.md
